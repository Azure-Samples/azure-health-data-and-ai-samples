# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-health-data-services-samples
metadata:
  template: azure-health-data-services-samples@0.0.1-beta
services:
  func:
    project: .
    language: csharp
    host: function
