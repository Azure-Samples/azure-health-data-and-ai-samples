{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to AzureAuth Custom Operation Azure Function",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}